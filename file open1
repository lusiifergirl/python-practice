import random
with open("matn.txt", "r", encoding="utf-8") as f:
    word = random.choice(f.read().split())
guess = input("enter your guess:")
horof = ''
for i in word:
    if i in guess:
        horof += i
    else:
        horof += "_"
print("horof:", horof)
